[phases.setup]
nixpkgsArchive = "51ad838b03a05b1de6f9f2a0fffecee64a9788ee"
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd maria_backend && NPM_CONFIG_PRODUCTION=false npm ci --cache /tmp/.npm --no-audit --no-fund",
  "cd web && NPM_CONFIG_PRODUCTION=false npm ci --cache /tmp/.npm --no-audit --no-fund"
]

[phases.build]
cmds = [
  "cd maria_backend && npm run prisma:generate",
  "cd maria_backend && npm run build",
  "cd web && npm run build",
  "rm -rf maria_backend/public/app && mkdir -p maria_backend/public/app",
  "cp -r web/dist/. maria_backend/public/app/"
]
